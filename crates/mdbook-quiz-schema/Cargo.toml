[package]
name = "mdbook-quiz-schema"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {version = "1.0.188", features = ["derive"]}
ts-rs = "7.0.0"

[dev-dependencies]
toml = { workspace = true }